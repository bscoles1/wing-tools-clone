# Protocol Explorer Verification

## Responsive review — 2026-08-13

| Viewport | Verified outcome |
|---|---|
| Desktop, 1440 × 900 | The four protocol fact cards remain in one row; the supplied internal-data tree and source-vs-input explanation form a balanced two-column reference area. The snapshot picker and OSC command reference remain available below the fold without clipping. |
| Mobile, 390 × 844 | The protocol facts stack as full-width cards with clear spacing. The heading, safety message, supplied visual, and command reference adapt to a single-column reading order without horizontal overflow. |

The protocol explorer is intentionally presented as an **offline reference**. It documents OSC paths derived from selected snapshot channels while explicitly avoiding direct console connections or remote writes.
