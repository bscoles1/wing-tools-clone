# Signal Flow Context and Path Tracing Verification

## Responsive Review — 2026-08-13

| Viewport | Outcome |
|---|---|
| Desktop, 1440 × 900 | The path-tracing legend remains on one horizontal context bar above the organization chart. The inspector stays alongside the chart, and stage controls remain visible. |
| Mobile, 390 × 844 | The legend wraps into a readable vertical stack beneath the routing counts. The source, selected, and downstream color keys remain legible without horizontal clipping. |

The selected-path behavior automatically opens every chart stage that contains a related upstream or downstream node. Focused nodes use indigo, upstream nodes use emerald, downstream nodes use sky, and unrelated visible nodes are subdued to preserve the selected route’s visual hierarchy.
